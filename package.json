{
  "name": "dividend-tracker",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute dividend-tracker-db --local --file=./schema.sql",
    "db:migrate:remote": "wrangler d1 execute dividend-tracker-db --remote --file=./schema.sql"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.112.3",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250110.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.99.0"
  }
}
